package carl.greedy;

public class Candy {
    public int candy(int[] ratings) {
        // 输入: [1,2,2]
        // 输出: 4
        int[] up = new int[ratings.length];
        int[] down = new int[ratings.length];
        for (int i = 0; i < ratings.length; i++) {
            //画图，然后考虑如何使用贪婪思想

        }
        return 0;
    }
}
